pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,json,md,yml,yaml}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true

---
name: __NAME__
description: __DESCRIPTION__ Use this skill when ... (replace with concrete triggers — this line is how Claude decides to invoke the skill).
---

# __NAME__

<!--
This is starter content — replace it with your skill's instructions.

Keep SKILL.md focused: state what the skill does and the steps to do it. Move
long reference material, examples, and scripts into sibling files and link to
them (progressive disclosure) so the skill stays small until it's needed:

    skills/__NAME__/
      SKILL.md          <- you are here
      reference.md      <- optional: deeper docs the agent reads on demand
      scripts/          <- optional: executable helpers
-->

## What this does

Describe the capability in a sentence or two.

## How to use it

1. First step.
2. Second step.

## Notes

- Edit the frontmatter `description` above to sharpen when Claude triggers this.
- Validate locally with `pnpm --filter __PKG__ test` (runs `aubron-skill validate`).

node_modules
dist
.turbo
coverage
*.log
.DS_Store
.claude/worktrees/
# Apps (Next.js / Vercel build artifacts). next-env.d.ts is committed on purpose
# so `tsc --noEmit` typecheck passes in CI without a prior `next build`.
.next
.vercel
*.tsbuildinfo
